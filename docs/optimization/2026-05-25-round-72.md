# 2026-05-25 持续优化第 72 轮

## 当前审查范围

本轮审查 `docs/release.md` 内部是否仍有旧的泛化设备验收日志路径。

## 发现

第 70 轮已经让 `verify-device.ps1` 默认日志目录包含 release 版本：

```text
build\device-verification\v<versionName>-<versionCode>-<timestamp>\
```

第 71 轮也同步了 README。但 `docs/release.md` 的发布前 checklist 第 9 步仍写 `build/device-verification/`，和后文测试机设备验收段不一致。

## 完成项

`docs/release.md`

- 将发布前 checklist 第 9 步的日志路径改为：

```text
build/device-verification/v<versionName>-<versionCode>-<timestamp>/
```

- 保持 checklist、测试机设备验收段和 README 的日志路径口径一致。

## 验证结果

引用检查：

```powershell
rg -n "build/device-verification|build\\device-verification|verification-run|v<versionName>-<versionCode>|发布前 Checklist|保留 .*日志" docs/release.md README.md docs/optimization/2026-05-25-round-71.md -S
```

结果：release checklist、测试机设备验收段和 README 都指向版本化设备验收日志。

空白检查：

```powershell
git diff --check -- docs/release.md README.md docs/optimization/2026-05-25-round-71.md
```

结果：通过，仅有 README 和 `docs/release.md` 的 LF/CRLF 工作区提示。

## 剩余风险

- 当前没有在线 Android 设备，无法实际生成版本化设备验收日志。
- 当前没有正式 release signing 环境，无法跑完整发布设备验收。

## 下一轮建议

继续审查 `smoke-release-bundle.ps1` 的自定义 `-ApksPath` 路径处理，确认自定义输出目录不存在时是否有清晰错误或自动创建目录。
