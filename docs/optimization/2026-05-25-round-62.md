# 2026-05-25 持续优化第 62 轮

## 当前审查范围

本轮审查 managed device 质量门和本地验证脚本的边界，重点是 README 是否清楚说明 `verify-local.ps1` 与 GitHub Actions 的验证范围差异。

## 发现

当前验证边界是：

- `scripts/test/verify-local.ps1`：校验 publish-safe 词库，运行 `ktlintCheck`、`detekt`、`testDebugUnitTest`、`assembleDebug`、`assembleDebugAndroidTest`。
- GitHub Actions：在上述基础上额外运行 `pixel2Api30DebugAndroidTest`。

README 原本列出了 CI 命令，但本地验证段没有明确说明：本地脚本只编译 androidTest APK，不启动 managed device。开发者可能误以为 `verify-local.ps1` 已经覆盖 managed device 执行。

## 完成项

`README.md`

- 在本地验证段补充：

```text
本地脚本只编译 androidTest APK，不启动 managed device；pixel2Api30DebugAndroidTest 由 GitHub Actions 运行，或在已配置模拟器环境时手动执行。
```

- 手动指定 JDK 示例同步补上 `assembleDebugAndroidTest`，避免和 `verify-local.ps1` 实际质量门不一致。

## 验证结果

引用检查：

```powershell
rg -n "本地脚本只编译 androidTest APK|pixel2Api30DebugAndroidTest|assembleDebugAndroidTest|verify-local" README.md .github\workflows\android.yml scripts\test\verify-local.ps1 -S
```

结果：README、workflow、`verify-local.ps1` 三处边界一致。

Diff 检查：

```powershell
git diff --check -- README.md docs\optimization\2026-05-25-round-61.md
```

结果：通过，仅有 LF/CRLF 提示。

## 剩余风险

- 本轮只改 README，没有实际运行 managed device；`pixel2Api30DebugAndroidTest` 仍需 GitHub Actions 或已配置模拟器环境确认。

## 下一轮建议

跑一次完整本地验证，给第 49-62 轮发布脚本、CI 配置、词库校验和文档调整建立新的本地基线。
