# 2026-05-25 持续优化第 33 轮

## 当前审查范围

本轮审查导出 JSON 协议说明是否和第 32 轮代码一致。代码已经新增 `dataIntegrity`，但设计文档仍只列到 `dailyStats`。

## 发现

`docs/plans/2026-05-16-english-vocabulary-app-development-design.md` 的“数据导出”章节没有记录 `dataIntegrity` 字段，也没有说明诊断失败时不阻断导出的降级语义。发布验收清单只写了 Settings 能导出 JSON，没有提醒检查导出文件是否带诊断摘要。

## 完成项

1. `docs/plans/2026-05-16-english-vocabulary-app-development-design.md`
   - 导出字段列表加入 `dataIntegrity`。
   - 补充 `status`、`cardsWithLogs`、`missingCacheCount`、`inconsistentCacheCount`、`legacyLogCardCount`、`orphanLogCount`、`repairableIssueCount`、`manualReviewIssueCount`、`issueCount`。
   - 明确诊断失败时 `status=unavailable`，不阻断导出。
2. `docs/release.md`
   - 设备验收项从“Settings 能导出 JSON”扩展为导出文件需包含 `dataIntegrity` 诊断摘要。

## 验证结果

文档 diff 检查：

```powershell
git diff --check -- docs/plans/2026-05-16-english-vocabulary-app-development-design.md docs/optimization/2026-05-25-round-32.md
```

结果：通过，仅有 CRLF/LF 提示。

## 剩余风险

- 这轮只改文档，没有重新跑 Gradle；第 32 轮已在同一代码状态下通过 `verify-local.ps1` 和未签名 release 构建。
- 仍无设备，无法实际导出文件并检查 `dataIntegrity`。

## 下一轮建议

1. 继续审查设置页导出和数据维护 UI 文案是否和 `dataIntegrity` 语义一致。
2. 有设备后执行导出 smoke，打开导出 JSON 验证 `dataIntegrity`。
