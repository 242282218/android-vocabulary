# 2026-05-25 持续优化第 37 轮

## 当前审查范围

本轮审查导出协议文档是否和第 36 轮代码一致，重点是 `latestImportRun.bookCounts` 的容错语义。

## 发现

导出代码已经包含 `latestImportRun`，并且 `vocabularyManifest` 与 `latestImportRun.bookCounts` 都支持“合法 JSON 导出为结构化对象，解析失败则保留原始字符串”。但设计文档的导出字段列表仍没有列出 `latestImportRun`，也没有说明诊断载荷的降级行为。

## 完成项

`docs/plans/2026-05-16-english-vocabulary-app-development-design.md`

- 导出字段列表补充 `latestImportRun`。
- 补充 `vocabularyManifest` 与 `latestImportRun.bookCounts` 的容错语义：合法 JSON 导出为结构化对象；无法解析时保留原始字符串，不能阻断用户导出复习数据。

## 验证结果

文档 diff 检查：

```powershell
git diff --check -- docs/plans/2026-05-16-english-vocabulary-app-development-design.md docs/release.md docs/optimization/2026-05-25-round-36.md
```

结果：通过，仅有 LF/CRLF 提示。

完整本地验证已在第 36 轮通过：

```powershell
.\scripts\test\verify-local.ps1
```

结果：通过。

## 剩余风险

- 本轮只同步文档，没有重新跑 Gradle。
- 仍无在线设备，无法打开真机导出文件检查字段。

## 下一轮建议

1. 继续审查首次启动词库导入错误信息，确认 assets 损坏时用户能看到明确失败原因。
2. 有设备后执行 Settings 导出 smoke，确认导出 JSON 可读且包含 `dataIntegrity`、`latestImportRun`。
