# 第 197 轮：修复 ScreenLayoutSmokeTest 的 ktlint 阻断，恢复 verify-local 全绿

## 审查证据

- `scripts/test/verify-local.ps1`
  - 本轮先跑完整本地验证链路，定位真实阻断项，而不是猜测局部模块。
- `app/build/reports/ktlint/ktlintAndroidTestSourceSetCheck/ktlintAndroidTestSourceSetCheck.txt`
  - 失败只集中在 `app/src/androidTest/java/com/zzz/androidvocab/ScreenLayoutSmokeTest.kt`
  - 具体规则：
    - `standard:argument-list-wrapping`
    - `standard:max-line-length`
    - `standard:function-signature`
- `app/src/androidTest/java/com/zzz/androidvocab/ScreenLayoutSmokeTest.kt`
  - 第 313 行把 5 个 `BookCode` 写在同一行，超过 120 列且违反参数换行规则。
  - 第 517 行把表达式函数体拆成两行，违反当前 ktlint 的 `function-signature` 规则。

结论：本轮阻断不是数据或业务回归，而是上轮 smoke test 修复后留下的格式问题，直接卡住了发布前本地验证链路。

## 方案

- 方案 A：只手工修这两处 ktlint 违规
  - 优点：最小修改，不会把自动格式化扩散到其他脏文件
  - 缺点：需要逐条对照规则手改
- 方案 B：对整个文件跑 `ktlintFormat`
  - 优点：快
  - 缺点：可能带出无关 diff，不符合当前外科手术式修改要求

采用方案 A。

## 修改

- `app/src/androidTest/java/com/zzz/androidvocab/ScreenLayoutSmokeTest.kt`
  - 将 `selectedBooks = listOf(...)` 改成多行列表，满足换行和长度限制。
  - 将 `findTextNode()` 改为单行表达式函数体，满足 `function-signature` 规则。

## 验证

1. 回跑完整本地验证：

```powershell
.\scripts\test\verify-local.ps1
```

结果：通过。

- release script regression：通过
- publish-safe vocabulary assets：通过
- `ktlintCheck detekt testDebugUnitTest assembleDebug assembleDebugAndroidTest`：通过

## 未验证项

- 本轮没有单独重跑 `connectedDebugAndroidTest`，因为 `verify-local.ps1` 已覆盖 androidTest APK 编译链路，本轮阻断也仅是 ktlint。
- 本轮没有重新跑 `verify-device.ps1`，因为上一轮已通过，且本轮只修改测试文件格式。

## 下一轮建议

- 继续优先看发布准备链路：
  - `build-release.ps1`
  - `build-bundle.ps1`
  - `verify-release-artifacts.ps1`
- 如果发布链路已无明显脚本阻断，再转向 Today/Review/Stats 的 UI/UX 细节和暗色模式验收。
