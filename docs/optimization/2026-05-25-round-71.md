# 2026-05-25 持续优化第 71 轮

## 当前审查范围

本轮审查 README 的设备验收日志说明是否与 `verify-device.ps1` 和 `docs/release.md` 的版本化日志口径一致。

## 发现

第 70 轮已将 `verify-device.ps1` 的默认日志目录改为：

```text
build\device-verification\v<versionName>-<versionCode>-<timestamp>\
```

并新增 `verification-run.txt` 记录版本、设备、AAB 路径和关键开关。

但 README 仍只写日志会写入 `build/device-verification/`，没有说明版本化子目录和 `verification-run.txt`。这会让 README 和 release 文档的精确程度不一致。

## 完成项

`README.md`

- 将完整设备验收日志说明同步为：

```text
build/device-verification/v<versionName>-<versionCode>-<timestamp>/
```

- 补充 `verification-run.txt` 会记录版本、设备和关键开关。

## 验证结果

引用检查：

```powershell
rg -n "device-verification|verification-run|v<versionName>-<versionCode>|verify-device\.ps1|日志" README.md docs/release.md scripts/test/verify-device.ps1 docs/optimization/2026-05-25-round-70.md -S
```

结果：README、release 文档和 `verify-device.ps1` 都包含版本化日志目录或 `verification-run.txt` 说明。

空白检查：

```powershell
git diff --check -- README.md docs/release.md scripts/test/verify-device.ps1 docs/optimization/2026-05-25-round-70.md
```

结果：通过，仅有 README 和 `docs/release.md` 的 LF/CRLF 工作区提示。

## 剩余风险

- 当前没有在线 Android 设备，仍无法实测 `verification-run.txt` 的真实落盘内容。
- 当前没有正式 release signing 环境，无法跑完整发布设备验收。

## 下一轮建议

继续审查 `docs/release.md` 内部是否还有旧的泛化日志路径，例如 checklist 是否也应改成版本化 `build/device-verification/v<versionName>-<versionCode>-<timestamp>/`。
